import styles from './pages.module.css'
export default function Settings() {

    return (
    <div id="settings" className={`settings content-box ${styles.displayNone}`}>
        <h3>Settings</h3>
        <p>Customize your preferences and settings for the platform.</p>
    </div>
    )
}
